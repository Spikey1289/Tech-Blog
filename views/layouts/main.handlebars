<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8"/>
        <meta name="viewport" content="width=device-width, initial-scale=1"/>
        <title>Tech Blog</title>
        <link href="
                https://cdn.jsdelivr.net/npm/bulma@1.0.0/css/bulma.min.css
                " rel="stylesheet">
        <link rel="stylesheet" href="/css/style.css"/>
    </head>

    <body>
        {{!-- title and nav bar --}}
        <header class="has-background-primary-00">

            {{!-- title --}}
            <div class="columns">
                <h1 class="column mt-1 is-one-third is-offset-1 title has-text-centered">The Tech Blog</h1>
            </div>

            {{!-- navbar --}}
            <div class="box">
                <nav class="columns is-spaced">
                    {{!-- loads page buttons the user has access to dynamically, based on if the user is logged in --}}
                    {{#if logged_in}}
                        <a class="column is-narrow" href="/">
                            <button class="button is-link homepage">Homepage</button>
                        </a>
                        <a class="column is-narrow" href="/dashboard">
                            <button class="button is-link dashboard">Dashboard</button>
                        </a>
                        <div class="column is-narrow">
                            <button class="logout button is-warning">Logout</button>
                            <script src="/js/logout.js"></script>
                        </div>
                    {{!-- if the user has not logged in, only displays the login button --}}
                    {{else}}
                        <a class="button is-primary column is-1" href="/login">
                            <button>LogIn</button>
                        </a>
                    {{/if}}
                </nav>
            </div>
        </header>

        {{!-- main body of handlebars --}}
        <main class="box has-background-primary-05">
            {{{body}}}
        </main>
    </body>
</html>